import React from 'react';
import '../css/Page.css';

function Manage() {
 
  return (
    <div className="Page">
      <h1>Manage</h1>
      {/* This page should feature two tabs, one that is create assets, one that is manage existing assets that lets you create more of given cards */}
      {/* Create assets: This should allow for naming a deck, deck mode, decimals etc. */}

      {/* Manage assets: Should let you select an asset you own and then create more or something similar */}
      </div>
  );
}

export default Manage;